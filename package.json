{
  "name": "personal-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node server.js",
    "start:production": "NODE_ENV=production STATIC_ROOT=dist/client node server.js",
    "preview": "STATIC_ROOT=dist/client node server.js",
    "test:e2e": "playwright test",
    "test:e2e:baseline": "playwright test tests/e2e/access-session-expiry.spec.js tests/e2e/resource-baseline.spec.js tests/e2e/resource-p0.spec.js",
    "test:e2e:p0": "playwright test tests/e2e/resource-p0.spec.js",
    "test:e2e:shell": "playwright test tests/e2e/resource-page-shell.spec.js",
    "check": "node --check app.js && node --check server.js && node --check server/access-auth.js && node --check server/deployment-security.js && node --check server/storage.js && node --check scripts/build.mjs && node --check scripts/check-build.mjs && node --check scripts/check-access-auth.mjs && node --check scripts/check-postgres-state.mjs && node --check scripts/manage-state-backups.mjs && node --check scripts/check-state-migration-backups.mjs && node --check scripts/audit-source.mjs && node scripts/audit-source.mjs && node scripts/check-access-auth.mjs",
    "check:build": "npm run build && node scripts/check-build.mjs",
    "verify": "npm run check && npm run check:build && npm run test:e2e:baseline",
    "check:postgres": "node --env-file-if-exists=.env scripts/check-postgres-state.mjs",
    "check:access-auth": "node scripts/check-access-auth.mjs",
    "check:backups": "node --env-file-if-exists=.env scripts/check-state-migration-backups.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "esbuild": "^0.28.1"
  }
}
