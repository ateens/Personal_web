# Resource / Notion parity final gap audit

Date: 2026-07-12 (Asia/Seoul)

Implementation: PR #1 merged the parity rebuild to `main` at `665b237`; PR #2 merged production auth hardening at `617f44e`; the current workspace additionally contains undeployed local Trash/recovery safety, docked-Full chrome, Lock/Move/Duplicate/Markdown export, hierarchy-queue corrections, URL Link/Bookmark/Embed paste choice, inline equation/color controls, cross-page block Move, comment-aware history/anchor repair, exact block deep links, and local-only comment read cursors

Audit point: the production access-control claims below apply to the recorded deployed revisions. The recorded 115/115 bundled-Chromium run and build metrics predate the newest URL/toolbar/block-Move/comment/deep-link/read-cursor tranche and are historical checkpoint evidence only. Focused suites cover the new behavior, but a fresh complete E2E run and all release gates must replace the stale totals before this worktree is described as a tested release candidate. None of these newer local changes is part of the deployed revision.

## Result at a glance

- `[Verified locally, fresh full run pending]` Every currently known functional P0 has an implementation and focused automated coverage: minimal Resource list/detail patching, IME-safe search/title editing, Resource v4 timestamps/revisions, durable local recovery, optimistic concurrency, keyboard-openable cards, parity/Advanced mode separation, Center/Side/Full shells, docked-Full workspace chrome, exact block deep links, Resource membership deletion guards, strict state validation, semantic block structure, and client/server URL allowlists. The newest tranche has focused coverage, but the last 115-test complete run predates it and must not be presented as current.
- `[Verified deployed]` PR #1 merged the Resource rebuild at `665b237`; PR #2 merged the exact-target one-way API verifier at `617f44e`. Railway deployment `32e9cf58-b0ca-4342-9638-e1bf078e071c` reported `ACTIVE` and `Deployment successful` during auth verification. Production migrated v3→v4 at revision 1, retains its manual predeploy backup, and reported revision 2 after the final authenticated smoke test.
- `[Verified]` Anonymous Sites requests to `/`, `/api/state/status`, and `/health` return `401`, so the deployed Worker gate is active for anonymous traffic.
- `[Verified]` The exact Railway project/environment/service target forces the committed SHA-256 verifier and state preconditions without storing the token in source. Direct Railway `/api/state/status` returns `401 AUTH_REQUIRED` for missing and wrong credentials and `200` for the matching Sites credential; authenticated state reads return `ETag: "state-2"` and `X-State-Concurrency: required`; `/health` remains `200` by design.
- `[Verified]` The signed-in owner Sites path returns `200`, reported revision 2, rendered the production Resources view, and opened a real Center peek. This proves the Worker secret still matches the deployed Railway verifier after enforcement.
- `[Verified]` Phase B's core is implemented: IndexedDB snapshot/operation stores, replay/retry/conflict behavior, truthful save states, Resource-level incremental writes, waiting service-worker update gating, migration snapshots, integrity-checked restore, and isolated database checks. Production v3→v4 migration revision 1 and the manual predeploy backup are also verified.
- `[Partial]` Phase A now covers the original/default Resource page layout, the existing editor feature set, selected-block actions including cross-page Move, URL Link/Bookmark/Embed choice, inline equation/colors, comment-aware block history, exact block deep links, and local-only comment read cursors. It does not cover every current Notion block, custom database page layouts, column construction, unified title/property/icon/cover/native-text history, or real multi-user collaboration.
- `[Verified reference, partial comparison]` An authenticated Notion desktop session supplied ephemeral/private 768×964 captures for database Side peek (`pm=s`), Center peek (`pm=c`), and Full page. The references verify the three static shell structures at that viewport and exposed a P0 breakpoint regression: this product had collapsed fine-pointer 768px Center and Side into the compact full-screen shell. The local correction preserves a centered modal and nonmodal resizable split Side at 768px; the newer docked-Full behavior is covered by focused desktop/compact tests and the passing full E2E run.
- `[Unverified]` The captured Notion session was dark-theme, Korean, and used a custom/pinned property layout. It is not a matched light-theme content/token baseline, and static images do not verify Back/Forward, focus, Escape order, resize behavior, interaction, or motion. Exact color, typography, shadow, radius, and the full paired state matrix therefore remain unverified. Raw Notion screenshots were not committed because they expose private workspace content.

## Reference surfaces and scope

The comparison keeps these surfaces separate:

1. authenticated database **Side peek**;
2. authenticated database **Center peek**;
3. authenticated **Full page**;
4. unauthenticated **Public Notion Site**.

The implemented opening policy is Library → Center, List → Side, and Map → Center by default, with Side/Center/Full selectable per Resource view. Map is treated as a product-specific view, not as a native Notion view. The implementation scope is the original/default Resource page layout; Notion's custom layout builder is P2.

The authenticated desktop-app reference was captured privately at 768×964 in a signed-in workspace: Side peek used the `pm=s` route, Center peek used `pm=c`, and Full page used its page route. The captures are retained only as ephemeral/private audit evidence; no raw screenshot is committed. Their dark theme, Korean locale, and custom/pinned properties layout make them suitable for structural shell comparison, not for generalizing Notion's default property layout or exact visual tokens.

The functional baseline comes from current official Notion documentation:

- database items are pages with properties and free page content: <https://www.notion.com/help/intro-to-databases>;
- Side peek, Center peek, Full page, opening defaults, database search, filters, and sorts: <https://www.notion.com/help/views-filters-and-sorts>;
- page fonts, small text, full width, icon, and cover behavior: <https://www.notion.com/help/customize-and-style-your-content>;
- keyboard and Markdown behavior: <https://www.notion.com/help/keyboard-shortcuts>;
- page discussions and inline comment threads: <https://www.notion.com/help/comments-mentions-and-reminders>;
- page links and backlinks: <https://www.notion.com/help/create-links-and-backlinks>;
- custom database page layouts, explicitly excluded from this checkpoint: <https://www.notion.com/help/layouts>.

Official documentation does not provide the authenticated editor's exact geometry or visual tokens. The 768×964 private captures add direct structural evidence for the three desktop shells, but a same-content, same-theme, same-locale, same-zoom paired matrix is still required for exact visual comparison.

## Final Gap Audit

Status meanings:

- `Resolved locally`: implementation and same-runtime automated evidence exist in the current workspace.
- `Partial`: the implemented subset is usable and tested, but a named requested capability remains.
- `Resolved production`: the current deployed revision and both access paths have direct live evidence.
- `Deferred P2`: deliberately outside the original/default Resource page scope.

| 우선순위 | 구성요소/상태 | 현재 구현 위치 | 현재 동작 | Notion 기준 동작 | 차이 | 수정/남은 조치 | 검증 방법/결과 | 직접 확인 여부 |
|---|---|---|---|---|---|---|---|---|
| P0 · Resolved locally | Resource 목록 최소 patch, focus/caret/IME | `app.js` `renderView({soft:true})`, `patchResourceView*`; `resource-baseline.spec.js`, `resource-p0.spec.js` | 검색 input, `#viewRoot`, Side database context를 유지하고 결과/control 단위만 patch한다. 필터·정렬·mode 변경도 열린 Side editor를 교체하지 않는다. | database control 입력과 열린 Side database 문맥이 연속 조작 가능해야 한다. | 로컬 기능 차이 없음. | 배포된 source revision과 테스트 evidence를 change record에 함께 보존한다. | focused identity coverage에서 input node identity, focus, caret, composition, scroll 및 filter/sort/mode identity 검사가 통과했다. | 제품 `[Verified]`; Notion 픽셀 `[Unverified]` |
| P0 · Resolved locally | Detail/property/title DOM 안정성 | `app.js` `patchResourceDetail*`, `patchBlockEditorStructure`, `renderEditorMutation`; `resource-dom-stability.spec.js` | property, URL, parent, comment soft mutation은 활성 editor node와 selection을 보존한다. 제목은 composition-aware draft 후 해당 제목 표시만 patch한다. Advanced mode의 다른 editor도 유지된다. | active page editor와 다른 열린 page의 편집 문맥이 유지돼야 한다. | 브라우저 native undo를 포함한 모든 page-level change의 단일 history는 아직 아님. | P1 history 통합은 별도 transaction 설계로 진행한다. | editor identity/selection 및 two-window non-interference 2건 통과. | 제품 `[Verified]` |
| P0 · Resolved locally and deployed | Resource v4 model, timestamp, revision | `app.js` `normalizeResourceRecord`, `touchResource`; `server/storage.js`; `check-postgres-state.mjs` | `createdAt`, `updatedAt`, `revision`, `timestampSource`, page fields를 migration한다. mutation만 timestamp/revision을 올리고 open/scroll은 올리지 않으며 updated sort에 안정적 tie-break를 사용한다. Railway deployed the server and migrated production v3→v4 at revision 1. | page 생성/수정 metadata와 실제 최근 수정 정렬이 필요하다. | legacy timestamp는 migration 시각이며 과거 실제 수정 시각으로 주장하지 않는다. | migration source label과 production backup을 보존하고 post-auth state checks를 다시 실행한다. | mutation/non-mutation E2E, isolated PostgreSQL check, production migration status 확인. | local/deployment `[Verified]` |
| P0 · Resolved locally | Durable local state, offline queue, conflict, SW update | `app.js` IndexedDB snapshot/operation/Resource-metadata stores; `service-worker.js`; `resource-offline.spec.js`, `resource-comment-read-cursor.spec.js` | online snapshot을 먼저 저장하고 first-ever offline pagehide, reload, replay, transient retry, stale conflict를 durable하게 처리한다. pending/conflict 중 SW reload를 막고 성공 뒤 적용한다. 댓글 read cursor는 workspace+Resource local metadata로 저장되어 locked/read-only page에서도 workspace write 없이 reload를 견딘다. | reload/network loss/update 중 draft를 잃지 않고 실제 상태를 표시해야 한다. | real OS crash, Safari storage eviction, long-lived multi-device recovery는 수동/field 검증 전이다. | production-like browser soak와 storage-eviction policy를 후속 검증한다. | offline spec 7개와 read-cursor focused cases가 현재 tree에 존재하며, fresh aggregate run은 pending이다. | Chromium focused `[Verified]`; field behavior `[Unverified]` |
| P0 · Resolved locally | Optimistic concurrency와 증분 저장 | `server.js` `/api/state`, `/api/resources/:id`; `server/storage.js`; `resource-revision-conflict.spec.js`, `resource-hierarchy-persistence.spec.js` | ETag/`If-Match`/`baseRevision`, optional `428`, stale `409`, explicit remote reload, Resource 단위 PUT을 제공한다. Hierarchy 변경은 old parent → moved Resource → new parent 순서와 stable `queueOrder`를 사용하고 reload, transient retry, Keep local rebase 뒤에도 순서를 보존한다. | stale writer가 최신 state를 조용히 덮어쓰지 않고 관계의 모든 중간 저장 상태가 유효해야 한다. | Block/Comment가 독립 endpoint/table transaction인 완전 세분화 모델은 아님. | payload/query telemetry 후 필요한 entity endpoint를 P1/P2로 분리한다. | two-writer coverage와 hierarchy persistence 6건 및 isolated PostgreSQL relation checks 통과. | `[Verified]` |
| P0 · Resolved locally | Resource membership deletion guard | `server.js` `handleStateWrite`; `server/storage.js` `writeAppState`; `resource-state-delete-guard.spec.js`; `check-postgres-state.mjs` | UI는 Resource hard-delete action을 노출하지 않고 `deleteEntity("resources", id)`를 거부한다. 공개 full-state API가 기존 Resource ID를 누락하면 row lock 안에서 `422 RESOURCE_PERMANENT_DELETE_DISABLED`로 전체 transaction을 거부한다. Incremental soft-trash/restore는 허용하고, 확인된 operator backup restore/reset 경로는 명시적 예외로 전체 membership을 교체할 수 있다. 사용자용 예제 데이터 동작은 기존 Resource를 지우는 reset이 아니라 비파괴적 보충이다. | 일반 사용자 삭제는 복구 가능해야 하고 영구 membership 교체는 권한 있는 복구/운영 경계로 분리돼야 한다. | 앱 사용자용 permanent delete는 의도적으로 지원하지 않으며 retention은 무기한이다. | 향후 permanent delete가 필요하면 별도 권한, 확인, retention, audit 정책과 endpoint를 먼저 정의한다. | delete-guard spec 3건과 isolated PostgreSQL omission/no-mutation/operator-reset checks 통과. | local `[Verified]`; production deployment `[Not deployed]` |
| P0 · Resolved locally and checkpointed | Migration backup/restore | `server/storage.js`; `scripts/manage-state-backups.mjs`; `check-state-migration-backups.mjs`; backup runbook | automatic migration/read-heal 전에 full authoritative snapshot을 같은 transaction에 저장한다. Manual create/list/restore는 workspace scope, SHA-256, revision precondition, safety backup, monotonic revision, restore history를 사용한다. A manual production predeploy backup exists. | migration 실패와 operator error에서 복구 가능한 checkpoint가 필요하다. | 같은 PostgreSQL 안의 backup이므로 DB/account loss는 막지 못하고 retention/prune도 의도적으로 없다. | backup metadata를 change record에 보존하고 별도 encrypted PITR/dump를 운영한다. | `npm run check:backups` isolated v3→v4/create/list/restore/sentinel cleanup 및 production manual checkpoint 확인. | `[Verified]` |
| P0 · Resolved production | 공개 API 인증과 workspace boundary | `server.js`; `server/deployment-security.js`; `worker/index.js`; API auth runbook | Sites version 8 keeps its secret and `REQUIRE_AUTHENTICATED_PROXY=1`; anonymous root/status are `401`. The exact Railway project/environment/service IDs force a one-way verifier and preconditions; missing/wrong bearer requests are `401`, matching bearer requests are `200`, state responses expose `ETag` and `X-State-Concurrency: required`, and health remains public `200`. 현재 모델은 하나의 `APP_STATE_ID`를 공유하는 single-workspace gate다. | 외부 노출 state API는 authenticated boundary 뒤에 있어야 한다. | Per-user/role/tenant authorization은 없고, Railway target IDs를 재생성하면 policy 상수 갱신이 필요하다. Production DB에 이전 `api_proxy_auth` row가 남아 있는지는 operator DB access 없이 확인하지 않았지만 exact-target 경로에서는 무시된다. | ID drift를 infrastructure change gate로 관리하고, token rotation 시 Sites secret과 verifier를 함께 갱신한다. DB operator access가 생기면 obsolete row를 audit/remove한다. | direct Railway `401/401/200`, authenticated state `200` + `"state-2"` + precondition header, anonymous Sites `401`, signed-in Sites status/Resources/Center peek `[Verified]`. | production `[Verified]` |
| P0 · Resolved locally | 서버 validation, URL/paste safety, abuse controls | `server.js`, `server/storage.js`, `app.js` URL/mark/comment normalizers; `check-postgres-state.mjs` | nested v4 schema, duplicate IDs, mark ranges, relation integrity, type/indent/size/depth, URL protocol을 mutation 전에 검증한다. Client는 http/https/mailto/tel 및 안전한 relative link만 anchor로 만든다. Bookmark/Embed require credential-free HTTPS and render without iframe/script/object/embed execution; supported inline color keys and lost-comment metadata are validated. Security headers, redacted errors/audit, route rate limits, write admission control을 제공한다. | hostile state/HTML/URL이 저장·실행되지 않고 API가 bounded해야 한다. | process-local limiter는 global DDoS/replica-wide quota가 아니다. HSTS는 HTTPS ingress 운영 확인과 함께 활성화해야 한다. Remote metadata, real iframe, and binary upload policies are intentionally absent. | deployed headers, ingress trust, 429 behavior, log redaction을 production에서 다시 확인한다. | focused URL/inline/comment checks pass; the combined isolated PostgreSQL gate must be rerun on the final source. | local focused `[Verified]`; aggregate DB gate `[Pending]` |
| P0 · Resolved locally | 검색 scope, filter logic, Library keyboard/grouping/header | `app.js` Resource controls and view renderers; `resource-baseline.spec.js`, `resource-p0.spec.js` | Database와 Full-text scope를 표시하고 분리한다. active/archive base scope와 추가 predicates는 AND로 결합한다. Library card는 stable Resource anchor이며 pinned/read-later/other bucket이 exclusive하고 header count copy를 표시한다. | database search 범위와 advanced filter logic이 명시되고 cards가 keyboard-accessible해야 한다. | cached excerpt의 view별 formatting/todo-aware preview는 제한적이다. | preview policy/index telemetry를 P1로 개선한다. | scope/filter/sort/view, Enter/Space, duplicate-ID absence, header copy tests 통과. | `[Verified]` |
| P0 · Resolved locally | Parity mode, page shells, URL/history/fallback | `app.js` router/page shell; `server.js`; `worker/index.js`; `service-worker.js`; shell, docked-Full, and block-deep-link specs | parity 기본값에서는 Advanced window chrome을 만들지 않는다. Center modal, desktop nonmodal Side, Full page, resize, previous/next, expand, copy link, Back/Forward, direct reload, invalid/trashed route를 제공한다. compact/coarse 화면의 Side는 full-screen modal로 전환된다. Full은 dock 여부와 관계없이 nav/shortcut 목적지와 history focus를 보존하며 skip link를 Resource surface로 전환한다. `#block-…` routes reveal collapsed ancestors without persistence, focus editable/divider/URL-preview targets, and avoid false success for missing hashes. | view별 Side/Center/Full 동작, database context, stable URL과 history가 필요하다. | 768×964 authenticated captures verify the static Side/Center/Full shell distinction, but exact visual tokens, interaction, and motion are not established. | matched state diff와 manual interaction/device evidence를 추가한다. | focused shell/docked-Full/deep-link coverage exists; the post-tranche complete run is pending. Authenticated static shell reference `[Verified]`. | product behavior `[Verified locally]`; exact Notion behavior `[Unverified]` |
| P0 · Resolved locally | 768px primary fine-pointer Center/Side breakpoint | `app.js` compact-shell media decision/modality sync; `styles.css` compact/coarse and 601–900px fine-pointer rules; shell/feature specs | 768px primary fine-pointer에서 Center는 centered modal sheet를 유지하고 Side는 database context를 남기는 nonmodal resizable split panel로 동작하도록 수정했다. 601–840px Side toolbar action은 panel 안에 유지되고, 840/841px Center geometry는 short viewport에서도 연속적으로 연결된다. 600px 경계를 넘을 때 `aria-modal`, initial focus, Tab trap, resize handle이 동기화되며 trash/restore shell replacement도 focus를 보존한다. 시각적으로 숨긴 save status는 accessibility tree에 남는다. | authenticated Notion 768×964 정적 캡처에서 Center는 centered sheet이고 Side (`pm=s`)는 원래 database context를 왼쪽에 남기는 split panel이다. 정적 캡처만으로 Notion의 modal semantics/resize 동작을 단정하지 않는다. | 이전 `max-width: 840px` 분기는 Center/Side/Full을 같은 full-screen shell로 붕괴시키고 제품 Side를 modal/non-resizable로 만들었으며, 첫 correction 뒤에는 narrow desktop toolbar overflow, stale modality/focus, trash focus loss, no-op resize control, save-status removal, 840/841px geometry jump가 남았다. | focused geometry/semantics/action-bounds/focus-trap/trash-focus assertions과 전체 viewport matrix를 유지한다. | authenticated static reference와 corrected product regression coverage `[Verified]`; Notion interaction semantics `[Unverified]` | product regression `[Verified locally]`; exact Notion interaction `[Unverified]` |
| P0 · Resolved locally | Semantic blocks와 기존 editor regression | `app.js` `renderBlock*`; editor matrix/transport/URL-paste specs | heading/blockquote/pre/code와 list/listitem semantics, named multiline textbox, stable block hooks를 제공한다. Markdown, split/merge, indent, shortcuts, selection, drag/copy/move, clipboard priority/sanitize, undo/redo, slash/mention/emoji/equation, Escape, title arrows, IME를 보존한다. Standalone HTTPS paste offers Link/Bookmark/Embed/Cancel and renders Bookmark/Embed as inert static previews. | page editor의 keyboard/selection/clipboard 동작과 semantic structure가 함께 작동해야 한다. | 모든 Notion block type, column drag, image/file paste, remote metadata/real iframe policy, large-paste progress는 없다. | 아래 P1/P2 편집기 항목으로 추적한다. | existing editor suites plus URL chooser 7/7 focused coverage; post-tranche full run pending. | existing scope `[Verified]` |
| P0 · Resolved locally | Permission read-only와 user Lock 분리 | `app.js` `resourceContentReadOnly`, `resourceMutationAllowed`, `setResourcePageLocked`; `resource-readonly.spec.js`, `resource-page-lock.spec.js` | `readOnly`는 permission policy이고 user-toggle `locked`는 별도 boolean이다. Locked page는 모든 content mutation을 차단하지만 Copy, Markdown export, page-menu 진입과 Unlock을 유지한다. `readOnly` page는 Copy/export는 가능하지만 Lock 상태를 바꿀 수 없다. | permission과 사용자 page lock은 서로 다른 상태이며 UI와 handler boundary가 일치해야 한다. | shared bearer model에는 Resource별 server-side ACL이 없다. | future multi-user scope에서 server authorization과 local lock을 분리해 유지한다. | lock/unlock/persistence/offline/server-type/forced-handler 5건과 read-only coverage 통과. | local single-workspace `[Verified]`; production deployment `[Not deployed]` |
| P1 · Resolved locally | Full page workspace chrome state | `app.js` routed Resource page controller; `styles.css` full shell; `resource-full-docked-nav.spec.js` | fine-pointer desktop 768px 이상에서 이미 dock된 nav를 보존하고 Full을 남은 폭에 배치한다. Full 동안 background main/FAB는 inert이고 sidebar navigation은 사용 가능하며, root destination push와 Back/Forward가 Full·focus·inert 상태를 복원한다. Nav가 미도킹이어도 Option shortcut과 일반 `setView`가 Full route를 종료하고 history focus를 보존한다. Compact/coarse에서는 Full이 viewport를 소유하고 sidebar까지 inert/covered하며, resize로 desktop/compact를 왕복해도 focus와 chrome state가 유지된다. Skip link는 Full의 focusable Resource surface를 가리킨다. | authenticated Notion Full은 workspace sidebar가 열려 있으면 sidebar를 보존하고 접으면 page가 전체 폭을 사용한다. | 제품 nav의 시각값은 Notion sidebar와 동일하다고 주장하지 않는다. | matched visual comparison과 real-device focus validation만 남는다. | docked/non-docked Full, history, resize, compact, skip-link 7건 통과. | behavior `[Verified locally]`; exact visual `[Unverified]`; deployment `[Not deployed]` |
| P1 · Partial | Properties, title, icon/cover, page menu | `app.js` Resource media/property/menu renderers; `resource-page-features.spec.js`, `resource-page-lock.spec.js`, `resource-hierarchy-persistence.spec.js` | switch/checkbox booleans, URL Open/Copy/Edit/Clear, collapsed hidden/inert properties, title, emoji icon, HTTPS cover, font/text/width settings과 실제 Copy link, Duplicate, Lock/Unlock, Move to submenu, Markdown-only export, Move to trash를 제공한다. Duplicate는 page/block에 새 ID를 부여하고 comments를 복제하지 않으며 새 deep link를 push해 Back으로 원본에 복귀한다. Page menu는 Tab 종료, ArrowRight 진입, 하위 Escape/ArrowLeft 복귀와 320px/짧은 landscape 스크롤을 지원한다. Markdown code fence는 본문의 최장 backtick run보다 길다. | property-specific controls와 실제 동작하는 page action이 keyboard/read-only state를 따라야 한다. | richer property popovers/order/hide policy와 page version history는 없다. Export는 의도적으로 Markdown만 지원한다. | richer property UX와 versioned page history를 별도 P1로 설계한다. | page feature, lock, hierarchy action specs 통과. | behavior `[Verified locally]`; Notion visual `[Unverified]`; deployment `[Not deployed]` |
| P1 · Partial | Hierarchy, mentions, backlinks | `app.js` hierarchy helpers and page Move submenu; `resource-hierarchy-persistence.spec.js`, `resource-page-command-mentions.spec.js`, page feature and trash specs | `parentId`, `childOrder`, real child creation, self/descendant cycle prevention, Resource mentions/backlinks, 실제 page Move submenu를 제공한다. Page Move는 old parent → moved Resource → new parent 순서로 저장하며 reload/retry/Keep local/PostgreSQL에서도 stable `queueOrder`와 관계 무결성을 유지한다. Resource/Project/Goal/Box/Task/Habit mentions navigate by click/Enter/Space and expose missing/trashed state where applicable. | page hierarchy, mentions, broken targets, backlinks가 실제 대상 관계여야 한다. | Cross-entity backlink surfaces/indexing beyond Resource backlinks are still absent. | Add cross-entity backlink indexing only after the product defines where those surfaces live. | hierarchy persistence, real child command, and all supported entity-mention navigation have focused coverage. | navigation `[Verified locally]`; cross-entity backlinks `[Pending]` |
| P1 · Partial | Page/inline comment threads | `app.js` comment thread model/pane, history snapshots, anchor reconciliation, local metadata store; comment/block/deep-link specs | page discussion과 inline range thread, replies, resolve/reopen/thread soft-delete를 분리한다. Comment changes undo/redo with blocks and threads together. Living marks rebase anchors; lost anchors become page threads with `anchorLostAt`/`formerAnchor`; orphan marks are removed; cross-page Move transfers anchored threads. Unread badges use a durable local-only workspace+Resource cursor and cause no workspace write, including locked/read-only reloads. 실제 identity가 없어 author를 만들지 않는다. | page/inline threads와 replies/status가 분리돼야 한다. | Reply-level deletion, author/permission/notification, and multi-user unread semantics are absent. | Keep the single-user cursor local-only; add attributed collaboration only after identity and authorization exist. | focused comment history/anchor, block action, cross-page Move, and read-cursor coverage exists; fresh complete run pending. | single-user scope `[Verified locally]` |
| P1 · Resolved locally | Trash, 복구, retention boundary | `app.js` Resource Trash filter/view/toast/recovery toolbar/orphan state; `server.js`, `server/storage.js`; `resource-trash-view.spec.js`, `resource-state-delete-guard.spec.js` | `trash` filter가 전용 Trash view를 열고 각 row에서 recovery page 또는 Restore를 제공한다. 삭제 직후 focus를 훔치지 않는 Undo action toast가 뜨며 keyboard Undo는 원래 focus를 복원한다. Recovery shell은 close/copy/restore/expand처럼 실제 recovery action만 표시하고, read-only 인접 row에서는 활성 opener로 focus를 옮긴다. Parent option과 Resource mention은 active/trashed/missing 상태를 표시한다. UI hard delete는 차단되고 공개 full-state Resource omission은 transaction 안에서 422로 거부된다. Operator restore/reset만 명시적 예외다. | soft trash/restore, 즉시 Undo, orphan 표시와 명확한 retention/permanent-delete policy가 필요하다. | 사용자 permanent delete는 의도적으로 제공하지 않고 retention은 무기한이다. | permanent delete가 제품 요구가 될 때 별도 권한·확인·retention·audit 정책을 먼저 정의한다. | Trash 6/6, state deletion guard 3/3, targeted PostgreSQL omission/operator-reset checks 통과. | local `[Verified]`; production deployment `[Not deployed]` |
| P1 · Partial | Slash/block/format actions | `app.js` menu/selection toolbar renderers; matrix, inline-toolbar, block-action, and cross-page-Move specs | slash/mention/emoji/equation menus는 stable IDs와 `aria-controls`, `aria-expanded`, `aria-activedescendant`를 연결하고 keyboard select/cancel을 지원한다. The selection toolbar adds inline equation plus nine text and nine background colors. The selected-block menu implements Copy link, Comment, within-page ordering, searchable cross-page Move, Copy, Duplicate, Delete, Turn into, and block colors; immutable destinations and ID collisions are rejected. | menus는 실제 지원 action만 제공하고 selection을 잃지 않아야 한다. | simple table/media/file/PDF/direct slash bookmark/embed/block equation/breadcrumb/TOC, recent/category UX, complete inline-link actions, and the column engine are absent. | schema/storage/render/export가 있는 action부터 단계적으로 추가한다. | toolbar 5/5, block action 3 focused cases, and six cross-page Move cases exist; fresh aggregate run pending. | implemented actions `[Verified locally]` |
| P1 · Partial | Unified undo/redo와 clipboard completeness | editor transaction/history, comment reconciliation, multi-Resource history, and transport code | structural block split/merge/duplicate/move/paste/delete, tested inline formatting, and page/inline comment add/reply/resolve/reopen/thread-delete synchronize DOM/state/save. Cross-page Move is one two-Resource transaction and transfers anchored threads. Duplicated/external clipboard blocks strip comment marks rather than reuse thread IDs. URL paste offers Link/Bookmark/Embed/Cancel. | text/title/property/icon/cover/comment를 포함한 예측 가능한 page history가 필요하다. | native text undo와 structural history가 완전히 하나가 아니며 title/property/icon/cover transactions are not integrated. Reply-level delete, image/file paste, and large-paste progress remain absent. | define the remaining page-level transaction/reload boundaries and binary upload model. | focused comment/block/URL/history coverage exists; fresh complete run pending. | current subset `[Verified locally]` |
| P1 · Partial | Mobile, accessibility, real-device input | `styles.css`; shell/editor specs; viewport matrix | 320–390px full-screen page flow, safe-area/visualViewport-aware layout, bottom editing toolbar, 44px controls, modal/nonmodal semantics, focus trap/return, keyboard resize, reduced motion, forced-colors focus, menu active descendant를 제공한다. | touch/keyboard/screen-reader 사용자에게 동일 기능 경로가 필요하다. | real iOS/Android soft keyboard, touch drag/long press, VoiceOver/TalkBack, contrast measurement은 자동 Chromium geometry만으로 확인할 수 없다. | device lab/manual screen-reader checklist를 실행한다. | required desktop/mobile geometry and keyboard E2E `[Verified]`; device/manual `[Unverified]` | automated Chromium `[Verified]`; real device/screen reader `[Unverified]` |
| P1 · Partial | Performance와 code boundaries | `app.js`; `resource-performance.spec.js` | the last recorded pre-tranche 400-block fixture measured 4,555 shell DOM nodes, 284 ms ready, 20.8 ms property patch, 16.4 ms scroll response, and zero observed long tasks. Minimal view/detail patch로 unrelated DOM churn을 줄였다. | long pages와 repeated edits가 responsive해야 한다. | single-file `app.js`와 non-virtualized 400-block DOM은 유지된다. The recorded headless numbers predate the newest tranche and are not production RUM. | rerun the performance spec on the final candidate, then add field telemetry before modularization/virtualization work. | historical local measurement only; current rerun pending. | current performance `[Unverified]` |
| P0 · Resolved production | Deployment, production migration, visual hand-off | merges `665b237` and `617f44e`, Railway deployment `32e9cf58-b0ca-4342-9638-e1bf078e071c`, Sites version 8, runbooks, `.openai/hosting.json` | Railway server, v4 migration, manual backup, exact-target auth, owner-only/custom Sites, anonymous gate, signed-in state path, Resources view, and Center peek are deployed and verified. | deployed candidate must match tested source and protect state before external use. | Authenticated static desktop references exist, but every newer local change described above—including Trash/guard, docked Full, page actions, hierarchy queue, URL/toolbar/block-Move/comment/deep-link/read-cursor work—is absent from production. The matched visual matrix and real-device/screen-reader gates also remain. | preserve deployed evidence; finish the fresh local gates; publish only through an explicitly authorized release; then repeat deployment smoke/auth/state checks. | deployed access-control/auth matrix `[Verified]`; newest local aggregate/release gates `[Pending]`; deployment `[Not deployed]`. | production access `[Verified]`; newer functions local only |
| P2 · Deferred | Custom database page layout builder | not implemented by scope | original/default Resource page layout only. | current Notion can configure heading pins, property groups, details panel, and Simple/Tabbed layouts. | entire builder/data model is outside Phase A. | separate product spec, migration, responsive and accessibility suite. | no decorative controls were added. | official behavior `[Verified]`; UI `[Unverified]` |
| P2 · Deferred | Additional blocks and columns | current block schema | existing paragraph/headings/lists/todo/toggle/quote/callout/divider/code set remains. Bookmark/Embed have validated schemas and inert renderers through the standalone HTTPS paste chooser. | broader Notion slash catalog and column layout exist. | simple table, image/video/audio/file/PDF, direct slash Bookmark/Embed, block equation, breadcrumb, TOC, database/synced/button/AI blocks, real remote metadata/iframe execution, and columns are absent. | add only with schema, sanitize, persistence, paste/export and accessibility support. | current block set plus URL chooser is focused-test covered. | current scope `[Verified locally]` |
| P2 · Deferred | Multi-user identity/roles/collaboration | single-workspace bearer model | authenticated proxy design gates one workspace but has no user authorization decisions. | true collaboration requires tenant/user/role/resource permission and attributed comments/presence/unread state. | per-user authorization and collaboration are absent by design. | confirm multi-user product scope before schema/UI; do not infer authorization from forwarded email alone. | auth self-test does not claim roles. | `[Verified]` absence |
| P2 · Deferred | Full modularization, virtualization, UUID migration | `app.js`, current schema | owner-scoped render/transaction helpers reduce churn without a framework rewrite. New IDs use collision-resistant generation where migrated paths support it; legacy IDs remain accepted under duplicate validation. | maintainable subsystem boundaries and long-document scaling are product engineering requirements, not visual parity alone. | monolith and unvirtualized document remain; a repo-wide legacy-ID migration is not complete. | extract store/repository/router/page/editor/selection/clipboard/a11y boundaries after behavior tests stay green. | source audit and 400-block test cover current risk. | `[Verified]` current state |
| P2 · Partially verified | Authenticated Notion pixel parity and dark mode | local product artifacts plus ephemeral/private authenticated references | 27 product viewport/mode captures plus Center properties/comments/menu and mobile comments were produced. Authenticated 768×964 Side (`pm=s`), Center (`pm=c`), and Full static references were inspected without committing their raw private-workspace images. | exact bounds, typography, color, border, shadow, interaction, and motion require matched authenticated same-state references. | the available Notion captures are dark-theme, Korean, and custom-property-layout references; the product fixture is not matched on theme/content/layout, and the complete state matrix is absent. | create privacy-safe matched pairs for the required states with the same content/theme/locale/zoom/viewport, then run measurable diffs and interaction checks. | authenticated static shell structure `[Verified]`; exact visual/behavior comparison `[Unverified]` | static structure `[Verified]`; exact parity `[Unverified]` |

## Automated evidence snapshot

The prior local checkpoint's full bundled-Chromium baseline reported **84 passed in 3.3 minutes**. That result predates the current Trash/guard, docked-Full, Lock/action, and hierarchy-queue changes and is retained only as historical regression evidence; it must not be presented as a full run of the current worktree.

The later **115/115 bundled-Chromium E2E run in 3.8 minutes** is also historical: it covered the Trash/guard, docked-Full, Lock/action, and hierarchy-queue checkpoint, but it predates the URL chooser, expanded inline toolbar, cross-page block Move, comment-anchor/history, exact block-deep-link, and local read-cursor work. It must not be labeled a current full-worktree result.

Current scoped evidence includes URL chooser 7/7, inline toolbar 5/5, cross-page block Move 6/6, comment history/anchor integrity 4/4 with a related 56/56 combined batch, and a 17-case deep-link/read-cursor integration set (block deep link 3, read cursor 4, block menu 3, page lock 5, read-only 2). The read-cursor tranche also passed 13/13 offline/hierarchy persistence regressions. Playwright discovery currently lists **162 tests in 27 files**, but discovery is not a pass result. These results are subsystem evidence only; the root task must rerun all 162 tests before release wording. The fixture server exposes an explicit production-write guard, so these browser runs do not mutate production.

| Suite | Count | Covered behavior |
|---|---:|---|
| historical bundled-Chromium E2E | 115 | pre-URL/toolbar/block-Move/comment/deep-link/read-cursor checkpoint; not current |
| current discovered E2E suite | 162 | 27 files; discovery only, fresh complete pass pending |
| current deep-link/read-cursor focused set | 17 | exact block focus/fallback, local metadata migration/upgrade/reload, zero-write lock/read-only behavior, related block-menu/permission regressions |
| current offline/hierarchy regression set | 13 | offline 7 plus hierarchy persistence 6 after the local metadata change |
| `resource-cross-page-block-move.spec.js` | 6 | searchable/clamped submenu, source→target ordering, multi-root/nested/all-block behavior, comment-thread transfer, collision no-mutation, multi-Resource undo/redo |
| `resource-url-paste-choice.spec.js` | 7 | Link/Bookmark/Embed/Cancel, keyboard/clamp, inert preview, transport/reload/undo, unsafe fallback and server validation |
| `resource-inline-toolbar.spec.js` | 5 | inline equation, 18 colors, keyboard semantics, persistence/history and viewport flip/clamp |
| `resource-comment-history-integrity.spec.js` | 4 | atomic comment lifecycle undo/redo, text-anchor rebase/loss, destructive block-mutation integrity, duplicate/clipboard mark ownership |
| `resource-trash-view.spec.js` | 6 | Trash-only drag target, dedicated view/restore, recovery-only toolbar, orphan state, keyboard Undo and adjacent-row focus, hard-delete guard |
| `resource-state-delete-guard.spec.js` | 3 | public full-state omission 422/no mutation, incremental trash/restore, trusted operator reset exception, non-destructive demo-data supplement |
| `resource-full-docked-nav.spec.js` | 7 | 768px docked Full geometry, inert/focus, docked/non-docked navigation history, skip link, desktop/compact transition |
| `resource-page-lock.spec.js` | 5 | `locked` versus `readOnly`, Copy/Lock/Unlock, forced-handler boundary, server type validation, offline replay |
| `resource-hierarchy-persistence.spec.js` | 6 | valid intermediate hierarchy, immutable picker rollback, Move submenu, Duplicate parent ordering, reload/retry queue order, Keep local rebase |

Recorded non-browser checks:

- The last isolated PostgreSQL checkpoint covered public full-state Resource omission rejection without mutation and the explicit trusted operator reset exception, in addition to incremental soft-trash/restore and hierarchy validation. The server schema changed afterward for URL blocks, inline colors, and lost-comment metadata, so the root task must rerun `npm run check:postgres`.
- The deep-link/read-cursor tranche passed `npm run check` and `git diff --check`; this is not a substitute for the final combined source/build/auth/PostgreSQL/backup gates.
- The previously recorded build (`1,162,032 → 815,222` bytes, Brotli `142,042`, gzip `181,108`) predates the newest tranche and is retained only as a historical checkpoint. The root task must run `npm run check:build` and replace these metrics rather than carrying them forward as current.
- `npm run check:api-auth`, `npm run check:backups`, and final `git diff --check` also require a fresh combined-candidate run.

## Viewport and visual evidence

`tests/e2e/resource-viewport-matrix.spec.js` exercises Center, Side, and Full at:

- 1440 × 1000
- 1280 × 900
- 1024 × 768
- 900 × 760
- 768 × 720
- 390 × 844
- 375 × 812
- 360 × 800
- 320 × 720

The recorded 27-state matrix stayed within the viewport and within the test's one-pixel horizontal-overflow tolerance at that checkpoint. The 768px fine-pointer regression assertion additionally verified that Center remained a centered modal and Side remained a nonmodal resizable split; compact/coarse screens used the full-screen modal path. A separate settled product matrix contains 19 implementation states and a contact sheet. Both artifacts predate at least the expanded inline toolbar, cross-page Move submenu, exact deep-link states, and local read/anchor-lost comment states; rerun them before describing screenshots as current.

Authenticated Notion desktop-app captures at 768×964 directly verify the static Side (`pm=s`), Center (`pm=c`), and Full shell structures. They are dark-theme, Korean, and based on a custom/pinned properties layout, while the product fixture is light-theme and not content-matched. Exact color, typography, shadow, radius, interaction, Back/Forward, focus/Escape, resize, motion, and the full state matrix remain unverified. Raw Notion images are deliberately absent from the repository because the captures contain private workspace content.

The privacy-safe geometry comparison at that viewport is narrower but concrete: the Notion Center sheet measured approximately `x=53, y=79, w=662, h=832`, while the corrected product shell measured `x=53, y=80, w=662, h=832`; the Notion Side boundary was approximately `x=345` with a `423×964` panel, matching the corrected product's `x=345, w=423, h=964`. These are historical outer-shell measurements only, not a pixel diff. Notion Full was directly checked with its workspace sidebar both open and collapsed: the open state retained an approximately 200px sidebar, while the collapsed state used the full 768px width. The local product code preserves an already-docked nav on fine-pointer desktop 768px and wider, keeps background main/FAB inert while sidebar navigation remains usable, and restores route/focus through Back/Forward. Compact/coarse Full covers and inerts the sidebar; desktop/compact resize restores the matching chrome. Exact visual parity remains unverified, the newest UI tranche needs regenerated product captures, and this behavior is not deployed.

## Deployment and rollback gates

The production access-control gate is enabled through the exact-target one-way verifier. The DB-backed policy remains an alternate mode and is ignored on the exact production target.

Completed and verified:

1. Final checks passed; PR #1 merged the Resource rebuild at `665b237`, and PR #2 merged production auth hardening at `617f44e`.
2. A production manual predeploy backup was created.
3. Railway auto-deployed the new server and migrated production state v3→v4 at revision 1.
4. The existing 32-byte random Sites credential matches the committed one-way verifier; the temporary recovery file was mode `0600` and was deleted after verification.
5. Sites version 8 was saved from `0bb41c3` and deployed owner-only/custom with environment revision 1, `API_BEARER_TOKEN`, and `REQUIRE_AUTHENTICATED_PROXY=1`.
6. Anonymous Sites `/`, `/api/state/status`, and `/health` return `401`.
7. Railway deployment `32e9cf58-b0ca-4342-9638-e1bf078e071c` was the successful auth-implementation deployment used for live verification.
8. Direct Railway status is `401/401/200` for missing/wrong/correct bearer credentials; authenticated state is revision 2 with `ETag: "state-2"` and `X-State-Concurrency: required`; `/health` is `200`.
9. Signed-in Sites status is `200` at revision 2, and the production Resources view plus a Center peek rendered successfully.

Non-blocking operational follow-ups:

1. Update the exact project/environment/service IDs and tests before any Railway resource recreation or cutover.
2. Rotate the Sites secret and committed verifier together; use a temporary dual-verifier rollout if zero downtime is required.
3. When production DB operator access is available, audit and remove any obsolete DB-backed plaintext `api_proxy_auth` row without weakening the active gate.
4. Confirm PostgreSQL PITR/dump coverage outside the application-managed backup tables.

For code/data rollback, stop or drain writes, deploy the matching code version, and restore only with exact workspace confirmation plus the current revision. Restore creates a safety backup and writes the restored content at `current revision + 1`. For authentication rollback, prefer the verified fail-closed environment override; do not remove the DB credential as the first response. The detailed procedures and limitations are in `resource-state-migration-backup-runbook-2026-07-11.md` and `resource-api-auth-runbook-2026-07-11.md`.

## Completion statement

The production access-control rollout remains verified complete for the recorded deployed revision. The newer local functional P0 work—including Trash membership safety, the 768px shell fixes, URL/toolbar/block-Move/comment/deep-link/read-cursor changes—has focused evidence, but the historical 115-test run and build metrics are no longer current; a fresh complete E2E and release-gate pass is required. Every new local feature remains undeployed. Phase A and the project as a whole are still not complete because matched authenticated state-by-state comparison, real mobile soft-keyboard/screen-reader QA, richer property popovers, cross-entity backlinks, reply-level comment deletion and multi-user identity semantics, page version history, and full block/media/paste/unified-history coverage remain open. This is not a claim of visual identity with Notion.
